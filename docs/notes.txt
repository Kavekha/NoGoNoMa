Load_raws
    Fusionner les raws pour ne pas les garder.
    Exemple: Table Raws oblige a chercher dans tous les raws pour determiner la table.
    Sanity checks: http://bfnightly.bracketproductions.com/rustbook/chapter_46.html
    Avoir les attaques speciales dans un Raw a part, pour mutualiser.

Tooltips:
    Mal formaté.
    Ne prends pas en compte la visibilité.
    Ne se retire pas quand curseur hors objet
    AwaitingInput: Enregistrer coordonnées + tooltip dans dico.
        Si coordonnées == mouse, on ne fait rien.
        Si coordonnées != mouse, on regarde si deja dans le dico + refresh
            Si pas dans le dico & tooltip, on créé le tooltip et on l'affiche

Input:
    Recuperation des inputs dans le save pour le Replay.

Initialisation
    Fichier a part.

AI:
    Meilleur pathfinding.
    Attiré par des items au sol.
    Actif / inactif selon presence pj.
    Eviter les NPC les uns dans les autres.

Tour par Tour:
    Deplacement impossible (dans le mur) compte comme un tour
    Si Cancel Targeting, un round est perdu pour le joueur. NPC agissent.

World:
    maintain :
        Gerer autrement les delete pour ne pas defoncer les iterators.
        On marque une entité comme "morte", et on l'efface en fin de cycle.

Logs:
    Morgue files & victory / Death screen.

UI:
    API pour mieux gerer l'interface vs Game.
    Meilleure disposition des menus.

MapGeneration:
    Rewrite map, trop de melange et de methodes au meme endroit
    Rooms avec 2 tiles d'ecart entre elles pour eviter les passages en diagonale

Map, visible tiles etc
    3 models : 1 en idx, 1 en xy et un en yx
    A uniformiser ou moyen de reduire erreur.
    (exemple: viewshed.visible_tiles est en yx)

Use:
    Fireball basé sur la visibilité de la cible, pas du joueur.
    Use system a refactor pour plus de lisibilité. Notamment le code equip / unequip, illisible.
    Items : Potion sans interdiction de boire alors que full pv.

Target
    Ne fonctionne plus dans certaines circonstances. Distance?
    Affichage degueulasse de la zone de ciblage.

Changer State
    Tjrs penible. cf damage_system pour changer l'etat de mort.

systems order

Inventory
    Max items, crash si letter index > 26

Localization
    Noms non traduits dans logs : items equipés, items ramassés, items droppés.
=====
